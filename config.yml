appName: Online Airline Ticket Booking Service
author: Ghazal Rafiei

database:
  
  database_name: TicketBooking

  port: 5432

  user: postgres

  password: Iran1234

  host: localhost

  schemas:

    Customer:
      columns:
      - firstname
      - surname
      - username
      - nationality_number
      - phone_number
      - password

    HostWebsite:
      columns:
      - website_address

    TravelAgency:
      columns:
      - name
      - address
      - phone_number
    
    TicketCollects:
      columns:
      - flight_time
      - destination
      - source
      - ticket_id
      - price
      - tclass
      - airline
      - website_address
      - travel_agency_name

    TransactionBuy:
      columns:
      - price
      - transaction_datetime
      - transaction_id
      - payment_service_provider
      - username
      - ticket_id
      - website_address
